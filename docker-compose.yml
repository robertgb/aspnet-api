services:
  webapi:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - "5000:8080"
    volumes:
      - ./src:/src
      - /src/bin
      - /src/obj
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://0.0.0.0:8080
